-
        name: Execute date command
        hosts: suse
        tasks:
                - name: Execute 'date' command
                  command: date
                  register: date
                  debug: var=date.stdout_lines

